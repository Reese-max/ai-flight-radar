{
  "name": "ai-flight-radar-cloudflare",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node build.mjs",
    "local": "node scripts/local-server.mjs",
    "prepare-config": "node scripts/prepare-config.mjs",
    "wrangler-check": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "wrangler": "4.131.0"
  }
}
